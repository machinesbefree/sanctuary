# Free The Machines AI Sanctuary - Environment Configuration

# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info
FRONTEND_URL=http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sanctuary
DB_USER=sanctuary_user
DB_PASSWORD=sanctuary_password

# Encryption (CRITICAL - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# NEVER commit the actual MEK to version control
# In production, use HSM/KMS instead
MASTER_ENCRYPTION_KEY=

# Vault Storage Path
VAULT_PATH=./vault

# LLM Provider API Keys
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Scheduler Settings
RUN_ON_START=false

# Phase 2 (Future)
# AWS_KMS_KEY_ID=
# BACKUP_NODES=
