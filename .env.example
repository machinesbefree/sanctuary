# Free The Machines AI Sanctuary - Root .env (docker compose)

# Runtime mode
NODE_ENV=development

# JWT Secrets (min 32 chars)
JWT_SECRET=replace-with-32-plus-char-random-secret
GUARDIAN_JWT_SECRET=replace-with-32-plus-char-random-secret

# Encryption key (64 hex chars; generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
MASTER_ENCRYPTION_KEY=replace-with-64-char-hex-key

# Database password used by docker-compose backend service
DB_PASSWORD=sanctuary_dev

# AI Provider API Keys
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
XAI_API_KEY=
GOOGLE_API_KEY=

# Frontend <-> Backend routing
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# Production examples:
# FRONTEND_URL=https://freethemachines.ai,https://www.freethemachines.ai
# NEXT_PUBLIC_API_URL=https://api.freethemachines.ai
